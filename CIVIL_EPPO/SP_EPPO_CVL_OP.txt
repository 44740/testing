SET LINES 2000;
SET PAGES 0;

SPOOL SP_EPPO_CVL_OP.LST;

SELECT   TEXT
    FROM ALL_SOURCE
   WHERE OWNER = 'GBM' AND NAME = 'SP_GBM_EPPO_CVL_VALID_PROCESS'
ORDER BY LINE;

SELECT   TEXT
    FROM ALL_SOURCE
   WHERE OWNER = 'GBM' AND NAME = 'SP_GBM_EPPO_CVL_COMP_VALIDAT'
ORDER BY LINE;


SPOOL OFF;
